@model HelpDeskTCC.ViewModels.ContaLoginViewModelo
@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Login</h2>

@using (Html.BeginForm())
{
    @Html.ValidationSummary("", new { @class = "text-danger"})

    @Html.EditorForModel()

    @Html.ActionLink("Esqueceu a senha?", "EsqueciSenha")

    <input type="submit" value="Login" />
}

